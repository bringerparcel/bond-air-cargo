import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Bond Air Cargo | Charter Freight & Cargo Solutions",
  description:
    "Bond Air Cargo — same-day charter, pharma logistics, medical transport, and gateway-to-gateway freight. From light urgent shipments to full freighter capacity.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.className}>
      <body>{children}</body>
    </html>
  );
}
